<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Warm gradient background -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="135"
                android:startColor="#FFB088"
                android:centerColor="#E8572A"
                android:endColor="#C84818" />
        </shape>
    </item>

    <!-- Subtle dot pattern overlay (just a darker blob) -->
    <item
        android:width="160dp"
        android:height="160dp"
        android:gravity="center">
        <shape android:shape="oval">
            <solid android:color="#22FFFFFF" />
        </shape>
    </item>

    <!-- Chef hat icon centered -->
    <item
        android:width="64dp"
        android:height="64dp"
        android:gravity="center"
        android:drawable="@drawable/ic_chef_hat" />

</layer-list>
